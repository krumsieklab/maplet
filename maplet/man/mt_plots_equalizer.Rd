% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_plots_equalizer.R
\name{mt_plots_equalizer}
\alias{mt_plots_equalizer}
\title{'Equalizer' plots}
\usage{
mt_plots_equalizer(
  D1,
  stat1,
  D2,
  stat2,
  legend_fine,
  legend_coarse = NULL,
  vline_fine = p.adj < 0.05,
  vline_coarse = p.adj < 0.05,
  color_list = c("#9494FF", "red")
)
}
\arguments{
\item{D1}{\code{SummarizedExperiment} input 1, the coarse one.}

\item{stat1}{Name of first comparison output to take arguments from, the coarse one [first one has to be the less
granular one (e.g. D1 super, D2 sub)].}

\item{D2}{\code{SummarizedExperiment} input 2, the fine one.}

\item{stat2}{Name of second comparison output to take arguments from, the fine one.}

\item{legend_fine}{Fine legend to be plotted.}

\item{legend_coarse}{Coarse legend to be plotted. Default: NULL.}

\item{vline_fine}{Filter expression where to draw the red, dashed line, for fine. Default: p.adj < 0.05.}

\item{vline_coarse}{Filter expression where to draw the red, dashed line, for coarse. Default: p.adj < 0.05.}

\item{color_list}{Colors for fine and coarse. Default: c("#9494FF","red") (light blue and red).}
}
\value{
$result$output: plot, equalizer
}
\description{
Creates a nested plot based on feature annotations, e.g. of super-/ and sub-pathways, or of sub-pathways and features.
}
\examples{
\dontrun{# super-pathway / sub-pathway equalizer
# sub-pathway analysis must already be stored in D_sub, and this is part of the super-pathway pipeline, with a result already in 'comp'
 ... \%>\%
 mt_plots_equalizer(stat1='comp',
                    D2=D_sub,
                    stat2=='comp',
                    legend_fine="sub pathway",
                    legend_coarse='super pathway',
                    vline_fine = p.adj < 0.1,
                    vline_coarse = p.adj < 0.1) \%>\%
 ...}

}
\author{
JK, MB
}
